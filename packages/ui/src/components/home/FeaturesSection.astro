---
import type { RegionConfig } from '../../types/region';
import { Star, ShieldCheck, Clock, Zap, Crown, Award } from 'lucide-astro';

interface Props {
  region: RegionConfig;
}

const { region } = Astro.props;
---

<section class="py-32 relative bg-slate-950 overflow-hidden">
    <!-- Section Header -->
    <div class="container mx-auto px-4 mb-20 text-center relative z-10">
        <h2 class="text-4xl md:text-6xl font-black text-white mb-6 tracking-tight">
            WHY <span class="text-red-600">CHOOSE</span> US
        </h2>
        <p class="text-slate-400 text-lg max-w-2xl mx-auto">
            {region.name} 최고의 퀄리티와 서비스를 약속드립니다.
        </p>
    </div>

    <!-- Bento Grid -->
    <div class="container mx-auto px-4 max-w-6xl">
        <div class="grid grid-cols-1 md:grid-cols-4 md:grid-rows-2 gap-6 h-auto md:h-[600px]">
            
            <!-- Bento Item 1: Large Feature (Spans 2 cols, 2 rows) -->
            <div class="md:col-span-2 md:row-span-2 relative group overflow-hidden rounded-[2rem] bg-slate-900 border border-slate-800">
                <div class="absolute inset-0 bg-gradient-to-br from-red-600/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
                <!-- Image/Visual Placeholder -->
                <div class="absolute inset-0 bg-[url('/noise.png')] opacity-20 mix-blend-overlay"></div>
                <div class="absolute bottom-0 left-0 right-0 p-8 md:p-12 z-10 bg-gradient-to-t from-slate-950 via-slate-950/80 to-transparent">
                    <div class="w-14 h-14 bg-red-600 rounded-2xl flex items-center justify-center text-white mb-6 shadow-lg shadow-red-600/30">
                        <Crown class="w-8 h-8" />
                    </div>
                    <h3 class="text-3xl font-bold text-white mb-4">Top 1% Official</h3>
                    <p class="text-slate-400 leading-relaxed text-lg">
                        엄격한 기준을 통과한 상위 1% 업소만을 선별하여 제휴합니다. 확실한 수질 보장.
                    </p>
                </div>
            </div>

            <!-- Bento Item 2: Wide Top Right (Spans 2 cols, 1 row) -->
             <div class="md:col-span-2 relative group overflow-hidden rounded-[2rem] bg-slate-900 border border-slate-800 p-8 flex flex-col justify-center">
                 <div class="absolute top-0 right-0 w-32 h-32 bg-red-500/10 rounded-full blur-3xl group-hover:bg-red-500/20 transition-colors"></div>
                 <div class="flex items-start justify-between gap-6 relative z-10">
                     <div>
                         <h3 class="text-2xl font-bold text-white mb-2">투명한 정찰제</h3>
                         <p class="text-slate-400">추가 금액 없는 투명한 가격 공개로 믿을 수 있습니다.</p>
                     </div>
                     <div class="w-12 h-12 bg-slate-800 rounded-xl flex items-center justify-center text-red-500 shrink-0">
                         <ShieldCheck class="w-6 h-6" />
                     </div>
                 </div>
             </div>

            <!-- Bento Item 3: Small Box (1 col) -->
            <div class="relative group overflow-hidden rounded-[2rem] bg-slate-900 border border-slate-800 p-8 flex flex-col items-center justify-center text-center hover:border-red-500/30 transition-colors">
                <div class="w-12 h-12 bg-slate-800 rounded-full flex items-center justify-center text-white mb-4 group-hover:scale-110 transition-transform bg-gradient-to-br from-slate-700 to-slate-800">
                    <Clock class="w-6 h-6" />
                </div>
                <h3 class="font-bold text-white text-lg">24/7 케어</h3>
                <p class="text-slate-500 text-sm mt-2">연중무휴 상담</p>
            </div>

            <!-- Bento Item 4: Small Box (1 col) -->
             <div class="relative group overflow-hidden rounded-[2rem] bg-gradient-to-br from-red-600 to-rose-700 p-8 flex flex-col items-center justify-center text-center text-white">
                <div class="w-12 h-12 bg-white/20 rounded-full flex items-center justify-center mb-4 backdrop-blur-sm">
                    <Zap class="w-6 h-6 fill-current" />
                </div>
                <!-- Animated Number -->
                <h3 class="font-black text-3xl mb-1">5min</h3>
                <p class="text-white/80 text-sm">평균 대기시간</p>
            </div>

        </div>
    </div>
</section>
