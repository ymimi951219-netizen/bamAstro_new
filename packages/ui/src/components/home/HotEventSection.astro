---
import { Timer, Gift, Sparkles, Scissors, QrCode } from 'lucide-astro';
---

<section class="py-24 bg-slate-950 relative overflow-hidden">
    <div class="container mx-auto px-4 max-w-4xl relative z-10">
        
        <!-- Ticket Container -->
        <div class="relative bg-white text-slate-900 rounded-3xl overflow-hidden shadow-2xl skew-y-[-2deg] hover:skew-y-0 transition-transform duration-500">
            <!-- Tear-off Line (Dashed) -->
            <div class="absolute top-0 bottom-0 left-[70%] w-0 border-l-2 border-dashed border-slate-300 z-20 hidden md:block"></div>
            <!-- Cutout Circles -->
            <div class="absolute -top-4 left-[70%] w-8 h-8 bg-slate-950 rounded-full z-20 hidden md:block -translate-x-1/2"></div>
            <div class="absolute -bottom-4 left-[70%] w-8 h-8 bg-slate-950 rounded-full z-20 hidden md:block -translate-x-1/2"></div>

            <div class="flex flex-col md:flex-row">
                <!-- Main Ticket Area (70%) -->
                <div class="p-8 md:p-12 md:w-[70%] bg-gradient-to-br from-white to-slate-100 relative">
                    <div class="inline-flex items-center gap-2 px-3 py-1 bg-red-600 text-white text-xs font-bold uppercase tracking-wider rounded mb-6">
                        <Sparkles class="w-3 h-3" />
                        한정 혜택
                    </div>
                    <h2 class="text-4xl md:text-6xl font-black text-slate-900 mb-4 tracking-tighter uppercase">
                        웰컴<br/><span class="text-red-600">드링크 50%</span>
                    </h2>
                    <p class="text-slate-600 text-lg mb-8 font-medium">첫 방문 고객님을 위한 웰컴 샴페인 반값 할인 혜택</p>
                    
                    <div class="flex items-center gap-4 text-sm text-slate-500 font-mono">
                        <div class="flex items-center gap-2">
                            <Timer class="w-4 h-4" />
                            <span>남은 시간: 04:59:12</span>
                        </div>
                        <div class="w-px h-4 bg-slate-300"></div>
                        <div>No. 0029384</div>
                    </div>
                </div>

                <!-- Stub Area (30%) -->
                <div class="p-8 md:w-[30%] bg-slate-50 flex flex-col items-center justify-center text-center border-t md:border-t-0 md:border-l border-dashed border-slate-200 relative">
                     <!-- Scissor Icon for visual cue -->
                    <div class="absolute top-1/2 -left-3 text-slate-400 hidden md:block">
                        <Scissors class="w-6 h-6 rotate-90" />
                    </div>

                    <div class="bg-white p-2 rounded-xl shadow-sm mb-4">
                        <QrCode class="w-24 h-24 text-slate-900" />
                    </div>
                    <p class="text-xs font-bold text-slate-400 uppercase tracking-widest mb-1">쿠폰 코드</p>
                    <p class="text-xl font-black text-red-600 font-mono">BAM-FIRST</p>
                    
                    <button class="mt-6 w-full py-3 bg-slate-900 text-white font-bold rounded-lg hover:bg-red-600 transition-colors shadow-lg">
                        혜택 받기
                    </button>
                </div>
            </div>
        </div>

        <p class="text-center text-slate-500/50 text-xs mt-8 uppercase tracking-[0.5em] font-light">약관 및 조건 적용</p>
    </div>
</section>
