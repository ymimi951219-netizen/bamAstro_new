---
import type { RegionConfig } from '../../types/region';

interface Props {
  region?: RegionConfig;
}

const { region } = Astro.props;
const imageName = region?.id ? `${region.id}-highpublic-karaoke-private-room.webp` : 'suwon-highpublic-karaoke-private-room.webp';
---

<section class="py-24 md:py-32 relative md:bg-fixed bg-center bg-cover bg-transparent">
  <div class="absolute inset-0 bg-slate-950/30 backdrop-blur-[1px]"></div>
  <div class="container mx-auto px-4 flex flex-col md:flex-row items-center gap-16 relative z-10">
    <div class="md:w-1/2 order-2 md:order-1">
      <span class="text-purple-500 font-bold tracking-widest uppercase mb-2 block">1인 방문 가이드</span>
      <h3 class="text-4xl md:text-5xl font-bold text-white mb-8 leading-tight">
        혼자 방문해도<br />편안합니다
      </h3>
      <div class="space-y-6 text-slate-300 text-lg font-light leading-relaxed">
        <p>
          최근 <strong class="text-white">1인 방문 고객이 40% 이상</strong>을 차지합니다.<br />
          혼자 오시는 분들도 어색하지 않도록 1:1 맞춤형 세팅과<br />
          프라이빗한 환경을 제공해 드립니다.
        </p>
        <p>
          조용히 대화를 나누고 싶은 날, 화끈하게 놀고 싶은 날,<br />
          원하시는 분위기만 말씀해 주세요. 고객님의 성향을 파악하여<br />
          가장 어울리는 파트너와 <strong class="text-purple-500">최적의 시간</strong>을 설계해 드립니다.
        </p>
      </div>

      <div class="mt-10 grid grid-cols-2 gap-4">
        {["1인 방문 절대 환영", "24시간 항시 대기", "프라이빗 룸 완비", "맞춤형 파트너 추천"].map((item) => (
          <div class="flex items-center gap-3 text-slate-300 bg-slate-900/50 p-3 rounded-lg border border-slate-800">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="text-purple-500 flex-shrink-0"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
            <span class="text-sm font-medium">{item}</span>
          </div>
        ))}
      </div>
    </div>
    
    <div class="w-full md:w-1/2 order-1 md:order-2 flex md:justify-center">
      <div class="relative">
        <div class="absolute -inset-2 md:-inset-4 bg-gradient-to-r from-purple-500 to-fuchsia-600 rounded-2xl opacity-30 blur-2xl animate-pulse"></div>
        <div class="aspect-[4/5] md:h-[500px] bg-slate-800 rounded-2xl overflow-hidden relative border border-slate-700 shadow-2xl md:transform md:rotate-2 hover:rotate-0 transition-transform duration-700">
          <img
            src={`/images/${imageName}`}
            alt="프리미엄 프라이빗 룸 인테리어 - 럭셔리한 조명과 고급 소파가 있는 VIP룸"
            class="absolute inset-0 w-full h-full object-cover"
            loading="eager"
            decoding="async"
            fetchpriority="high"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-slate-950/60 via-transparent to-transparent"></div>
          <div class="absolute bottom-0 p-8">
            <h3 class="text-3xl font-bold text-white">프라이빗 룸</h3>
            <p class="text-purple-500 font-bold tracking-widest uppercase text-sm">럭셔리 & 시크릿</p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
