---
import type { RegionConfig } from '../../types/region';
import { Phone, MessageCircle, MapPin, ChevronRight, ShieldCheck } from 'lucide-astro';

interface Props {
  region: RegionConfig;
}

const { region } = Astro.props;

const currentYear = new Date().getFullYear();
---

<footer class="bg-transparent border-t border-white/5 pt-16 pb-8 relative overflow-hidden">
  <!-- Backdrop Blur for legibility -->
  <div class="absolute inset-0 bg-slate-950">
    <div class="absolute inset-0 bg-[url('/grid-pattern.svg')] opacity-5"></div>
    <div class="absolute bottom-0 left-0 w-[500px] h-[500px] bg-red-900/10 blur-[100px] rounded-full"></div>
    <div class="absolute top-0 right-0 w-[500px] h-[500px] bg-slate-900/50 blur-[100px] rounded-full"></div>
  </div>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-12 mb-12">
      <div>
        <a href="/" class="flex items-center gap-3 mb-6">
          <div class="h-10 w-10 rounded-xl bg-slate-800/80 border border-white/10 flex items-center justify-center shadow-lg">
            <img src="/logo.webp" alt={`${region.name} 로고`} class="w-6 h-6 object-contain" />
        </div>
        <span class="text-2xl font-bold text-white tracking-tight">
            {region.name} <span class="text-transparent bg-clip-text bg-gradient-to-r from-red-400 to-rose-500">유흥 라운지</span>
        </span>
    </a>
    <p class="text-slate-400 mb-6 leading-relaxed">
        {region.name} 1등 룸싸롱/가라오케 예약 플랫폼.<br />
        투명한 가격과 최상의 서비스로 모시겠습니다.
    </p>
    <div class="flex items-center gap-4">
        <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-red-500/10 border border-red-500/20 text-red-400 text-xs font-bold uppercase tracking-wider">
            <ShieldCheck class="w-3.5 h-3.5" />
            공식 파트너
        </span>
        <span class="inline-flex items-center gap-1.5 px-3 py-1 rounded-full bg-slate-800 border border-slate-700 text-slate-300 text-xs font-bold uppercase tracking-wider">
            Since 2024
        </span>
    </div>
      </div>
      <div>
        <h3 class="font-bold text-white text-lg mb-6">프리미엄 연락처</h3>
            <ul class="space-y-3">
                <li class="flex items-center gap-3 text-slate-400 group">
                    <div class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center group-hover:bg-red-500/10 group-hover:text-red-400 transition-colors">
                        <Phone class="w-4 h-4" />
                    </div>
                    <a href={`tel:${region.phone}`} class="hover:text-white transition-colors font-medium">{region.phone}</a>
                </li>
                <li class="flex items-center gap-3 text-slate-400 group">
                    <div class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center group-hover:bg-yellow-500/10 group-hover:text-yellow-400 transition-colors">
                        <MessageCircle class="w-4 h-4" />
                    </div>
                    <a href={region.kakaoLink} target="_blank" class="hover:text-white transition-colors">카카오톡 문의</a>
                </li>
                <li class="flex items-center gap-3 text-slate-400 group">
                    <div class="w-8 h-8 rounded-full bg-slate-800 flex items-center justify-center group-hover:bg-blue-500/10 group-hover:text-blue-400 transition-colors">
                        <MapPin class="w-4 h-4" />
                    </div>
                    <span>{region.location}</span>
                </li>
            </ul>
        </div>

        <!-- Quick Links -->
        <div>
            <h3 class="font-bold text-white text-lg mb-6">바로가기</h3>
            <ul class="space-y-3">
                {['홈', '룸싸롱 가이드', '이벤트', '자주 묻는 질문', '예약하기'].map((item) => (
                    <li>
                        <a href="#" class="text-slate-400 hover:text-red-400 transition-colors flex items-center gap-2 group">
                            <ChevronRight class="w-3 h-3 text-slate-600 group-hover:text-red-500 transition-colors" />
                            {item}
                        </a>
                    </li>
                ))}
            </ul>
        </div>

        <!-- Venue Types -->
        <div>
            <h3 class="font-bold text-white text-lg mb-6">주요 업종</h3>
            <ul class="grid grid-cols-2 gap-x-4 gap-y-2">
                {region.venueTypes.slice(0, 8).map((type) => (
                    <li>
                        <a href={`/${type.slug}`} class="text-slate-400 hover:text-red-400 transition-colors flex items-center gap-2 group text-sm">
                            <div class="w-1.5 h-1.5 rounded-full bg-slate-700 group-hover:bg-red-500 transition-colors shrink-0"></div>
                            {type.name}
                        </a>
                    </li>
                ))}
            </ul>
        </div>
    </div>
    <div class="border-t border-white/5 pt-8 text-center text-xs text-slate-500">
      <div class="flex flex-col md:flex-row justify-center items-center gap-4 mb-4">
        <a href="/terms" class="hover:text-slate-300">이용약관</a>
        <span class="hidden md:inline">|</span>
        <a href="/privacy" class="hover:text-slate-300">개인정보처리방침</a>
      </div>
      <p>&copy; {currentYear} {region.name} 서우실장. 모든 권리 보유.</p>
    </div>
  </div>
</footer>
