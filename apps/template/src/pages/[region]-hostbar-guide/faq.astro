---
import { PageLayout, LocalBusinessSchema, FAQPageSchema, BreadcrumbSchema, BackToPillar, RelatedPosts } from '@bamastro/ui';
import { region } from '@/config/region';
import { Phone, ChevronRight } from 'lucide-astro';

export function getStaticPaths() {
  return [
    { params: { region: region.id } },
  ];
}

const { region: regionId } = Astro.params;

const faqList = [
    {
        question: `${region.name} 호빠 1인 방문도 가능한가요?`,
        answer: "네, 가능합니다. 1:1 응대도 부담 없이 안내해 드리며, 혼자 오시는 분들도 많습니다."
    },
    {
        question: `${region.name} 호빠 픽업은 어디까지 지원되나요?`,
        answer: `픽업은 ${region.name} 주요 역 및 중심 상권에서 운영됩니다. 정확한 동선은 예약 시 조율해 드립니다.`
    },
    {
        question: `${region.name} 호빠 주대/TC 기준이 궁금해요.`,
        answer: "주대 18만원 이상, TC 10만원 이상 기준이며 시간·코스·인원에 따라 달라질 수 있습니다. 상세는 문의 부탁드립니다."
    },
    {
        question: "초이스는 어떻게 진행되나요?",
        answer: "룸에 입장하시면 5~10조의 선수들을 차례로 보여드립니다. 마음에 드는 선수가 없으면 '패스'하셔도 되며, 이상형을 찾을 때까지 무한 초이스가 가능합니다."
    },
    {
        question: "복장 제한이 있나요?",
        answer: "특별한 드레스 코드는 없습니다. 편하게 오시면 됩니다. 다만 깔끔한 캐주얼을 추천드립니다."
    },
    {
        question: "타임 연장은 어떻게 하나요?",
        answer: "기본 타임은 1시간~1시간 30분입니다. 타임 종료 10분 전에 연장 여부를 여쭤봅니다. 풀티(Full-T)로 마감까지 함께하는 것도 가능합니다."
    },
    {
        question: "예약 없이 방문해도 되나요?",
        answer: `가능하지만 ${region.name} 상권 특성상 대기가 발생할 수 있어 예약을 추천드립니다. 미리 예약하시면 동선과 선수 배정을 맞춰드립니다.`
    },
    {
        question: "프라이버시는 어떻게 보장되나요?",
        answer: "철저한 비밀 보장을 원칙으로 합니다. 픽업 시에도 눈에 띄지 않는 동선으로 안내해 드립니다."
    }
];

const seoProps = {
  title: `${region.name} 호빠 FAQ | 자주 묻는 질문과 답변`,
  description: `${region.name} 호빠(호스트바) 자주 묻는 질문 완벽 해답. 1인 방문·초이스·예약·가격 기본 정보. 전문가가 답변합니다. 여성분 필독!`,
  keywords: [`${region.name} 호빠 FAQ`, `${region.name} 호빠 자주 묻는 질문`, `${region.name} 호스트바 예약`, `${region.name} 호빠 가격`],
};

const relatedItems = [
  {
    title: `${region.name} 호빠 완벽 가이드`,
    href: `/${region.id}-hostbar-guide`,
    description: '호빠 시스템, 가격, 추천 업소 정보',
    category: '호빠'
  },
  {
    title: `${region.name} 가라오케 가이드`,
    href: `/${region.id}-karaoke-guide`,
    description: '노래와 파티를 즐기는 가라오케',
    category: '가라오케'
  },
  {
    title: `${region.name} 하이퍼블릭 가이드`,
    href: `/${region.id}-highpublic-guide`,
    description: '프라이빗 펍, 비즈니스 접대에 최적',
    category: '하이퍼블릭'
  },
];
---

<PageLayout {...seoProps} region={region}>
    <LocalBusinessSchema region={region} />
    <BreadcrumbSchema region={region} />
    <FAQPageSchema items={faqList} />

    <div class="pt-24 md:pt-32 min-h-screen bg-transparent px-4 pb-20">
        <div class="container mx-auto max-w-4xl">
            <BackToPillar
                href={`/${region.id}-hostbar-guide`}
                title={`${region.name} 호빠 완벽 가이드`}
                description="가격, 시스템, 추천 업소 정보 모두 보기"
                accentColor="rose"
            />

            <div class="text-center mb-16">
                <div class="flex justify-center mb-4">
                    <div class="w-1.5 h-12 bg-rose-500 rounded-full"></div>
                </div>
                <h1 class="text-3xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-rose-200 to-rose-500 mb-6">
                    호빠 A to Z (FAQ)
                </h1>
                <p class="text-slate-400 text-lg leading-relaxed max-w-2xl mx-auto">
                    여성 전용 프리미엄 라운지. <strong class="text-white">{region.name} 호빠</strong>에 대한 모든 궁금증을 해결해 드립니다.
                </p>
            </div>

            <div class="grid gap-6">
                {faqList.map((item) => (
                    <div class="bg-slate-900/50 p-6 rounded-2xl border border-slate-800 hover:border-rose-500/30 transition-all duration-300">
                        <h3 class="text-lg md:text-xl font-bold text-rose-400 mb-3 flex items-start gap-2">
                            <span class="bg-rose-500/10 p-1 rounded text-sm mt-1">Q</span>
                            {item.question}
                        </h3>
                        <p class="text-slate-300 leading-relaxed pl-8">
                            <span class="font-bold text-slate-500 mr-2">A.</span>
                            {item.answer}
                        </p>
                    </div>
                ))}
            </div>

            <div class="mt-16 text-center">
                <p class="text-slate-400 mb-6">더 궁금한 점이 있으신가요? 24시간 언제든 물어보세요.</p>
                <div class="flex flex-col md:flex-row justify-center gap-4">
                    <button onclick={`window.location.href = 'tel:${region.phone}'`} class="bg-rose-600 hover:bg-rose-700 text-white font-bold py-4 px-8 rounded-xl flex items-center justify-center gap-2 transition-all shadow-lg hover:shadow-rose-500/25">
                        <Phone size={20} /> 실시간 예약 문의
                    </button>
                    <a href={`/${region.id}-hostbar-guide`} class="bg-slate-800 hover:bg-slate-700 text-white font-bold py-4 px-8 rounded-xl flex items-center justify-center gap-2 transition-all">
                        <ChevronRight size={20} /> 호빠 메인으로
                    </a>
                </div>
            </div>

            <RelatedPosts items={relatedItems} title="다른 업종도 확인해보세요" accentColor="rose" />
        </div>
    </div>
</PageLayout>
