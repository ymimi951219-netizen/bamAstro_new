---
import { PageLayout, LocalBusinessSchema, BreadcrumbSchema, HowToSchema, FAQPageSchema, VenueCard, TableOfContents, RelatedServices } from '@bamastro/ui';
import { region } from '@/config/region';
import { getVenueContent } from '../../content';
import { Sparkles, Users, Star, HelpCircle, Phone, CheckCircle, ChevronRight, GlassWater, Users, Star } from 'lucide-astro';

export function getStaticPaths() {
  return [{ params: { region: region.id } }];
}

const venueId = 'chaksukppa';
const venueType = region.venueTypes.find(v => v.id === venueId);
if (!venueType) throw new Error(`Venue type '${venueId}' not found`);

const content = getVenueContent(venueId)!;
const venueName = venueType.name;

const seoProps = {
  title: `${region.name} 착석빠 완벽 가이드 | 가격·시스템·추천 업소`,
  description: `${region.name} 착석빠 완벽 가이드. ${region.nearbyStations.slice(0,2).join('·')} 중심 착석 바, 가격, 예약, 이용 팁 상세 안내.`,
  keywords: [
    `${region.name} 착석빠`,
    `${region.name} 착석빠 가격`,
    `${region.name} 착석빠 추천`,
    `${region.name} 착석빠 후기`
  ],
};

const introSentences = content.price.intro.split('.').slice(0, 2).join('.') + '.';

const usageSteps = content.howToUse.steps.slice(0, 5).map(step => ({
  name: step.title,
  text: step.description
}));

const faqList = content.price.faqs.slice(0, 3).map(faq => ({
  question: faq.q,
  answer: faq.a
}));

const sections = [
  { id: "about", title: `1. ${region.name} 착석빠란?` },
  { id: "features", title: "2. 주요 특징 및 장점" },
  { id: "usage", title: "3. 이용 방법 (5단계)" },
  { id: "pricing", title: "4. 가격 안내" },
  { id: "ranking", title: "5. 추천 업소 TOP 3" },
  { id: "tips", title: "6. 이용 꿀팁" },
  { id: "faq", title: "7. 자주 묻는 질문" }
];
---

<PageLayout {...seoProps} region={region}>
    <LocalBusinessSchema region={region} />
    <BreadcrumbSchema region={region} />
    <HowToSchema
        name={`${region.name} 착석빠 이용 방법`}
        description="착석빠 이용 가이드. 예약부터 결제까지 완벽 안내."
        steps={usageSteps}
    />
    <FAQPageSchema items={faqList} />

    <div class="pt-24 md:pt-32 min-h-screen bg-transparent">
        <div class="container mx-auto px-4 pb-12 max-w-6xl">
            <div class="text-center mb-16 relative">
                <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 w-24 h-24 bg-purple-500/10 rounded-full blur-2xl"></div>
                <span class="text-amber-400 font-bold tracking-[0.2em] text-sm md:text-base uppercase mb-3 animate-fade-in-up block">착석 바</span>
                <h1 class="text-4xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-white via-slate-200 to-slate-400 relative z-10 animate-fade-in-up delay-100 drop-shadow-sm">{region.name} 착석빠 완벽 가이드</h1>
                <div class="w-1 h-12 bg-gradient-to-b from-amber-500 to-transparent mx-auto mt-6"></div>
            </div>

            <div class="mb-20 text-center max-w-4xl mx-auto animate-fade-in-up delay-200">
                <p class="text-xl text-slate-300 leading-relaxed font-light">
                    {introSentences}
                </p>
            </div>

            <TableOfContents sections={sections} />

            <div id="about" class="mb-12 bg-slate-900/30 p-8 rounded-2xl border border-slate-800/50 backdrop-blur-sm hover:border-purple-500/20 transition-colors animate-fade-in-up delay-300">
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                    <div class="w-1.5 h-8 bg-purple-500 rounded-full"></div>
                    1. {region.name} 착석빠란?
                </h2>
                <div class="text-slate-300 leading-relaxed text-lg font-light space-y-4">
                    <p>
                        <strong>{region.name} 착석빠은(는) {region.nearbyStations.slice(0,2).join('·')} 일대의 착석 바 공간입니다.</strong> 고품질 서비스와 편안한 분위기를 제공하며, 특별한 경험을 원하는 고객들에게 인기가 높습니다.
                    </p>
                </div>
            </div>

            <div id="features" class="mb-12 animate-fade-in-up delay-300">
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                    <div class="w-1.5 h-8 bg-purple-500 rounded-full"></div>
                    2. 주요 특징 및 장점
                </h2>
                <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                    <div class="bg-slate-900/50 p-6 rounded-xl border border-slate-800 hover:border-purple-500/50 transition-all">
                        <GlassWater class="w-10 h-10 text-purple-500 mb-4" />
                        <h3 class="text-xl font-bold text-white mb-3">편안한 착석</h3>
                        <p class="text-slate-400 text-sm leading-relaxed">
                            넓은 착석 공간에서 편안하게 시간을 보낼 수 있습니다.
                        </p>
                    </div>
                    <div class="bg-slate-900/50 p-6 rounded-xl border border-slate-800 hover:border-purple-500/50 transition-all">
                        <Users class="w-10 h-10 text-purple-500 mb-4" />
                        <h3 class="text-xl font-bold text-white mb-3">다양한 주류</h3>
                        <p class="text-slate-400 text-sm leading-relaxed">
                            와인, 칵테일, 위스키 등 다양한 주류를 구비하고 있습니다.
                        </p>
                    </div>
                    <div class="bg-slate-900/50 p-6 rounded-xl border border-slate-800 hover:border-purple-500/50 transition-all">
                        <Star class="w-10 h-10 text-purple-500 mb-4" />
                        <h3 class="text-xl font-bold text-white mb-3">프라이빗 공간</h3>
                        <p class="text-slate-400 text-sm leading-relaxed">
                            독립된 공간으로 프라이버시가 보장됩니다.
                        </p>
                    </div>
                </div>
            </div>

            <div id="usage" class="mb-12 bg-slate-900/30 p-8 rounded-2xl border border-slate-800/50 backdrop-blur-sm hover:border-purple-500/20 transition-colors">
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                    <div class="w-1.5 h-8 bg-purple-500 rounded-full"></div>
                    3. 착석빠 이용 방법 (5단계)
                </h2>
                <ol class="list-decimal pl-6 space-y-4 text-slate-300">
                    {content.howToUse.steps.slice(0, 5).map((step) => (
                        <li>
                            <strong class="text-white">{step.title}</strong> - {step.description}
                            {step.tip && <div class="text-sm text-purple-400 mt-1">💡 Tip: {step.tip}</div>}
                        </li>
                    ))}
                </ol>
                <div class="mt-6 text-center">
                    <a href={`/${region.id}-chaksukppa-guide/how-to-use`} class="inline-flex items-center gap-2 text-purple-400 hover:text-purple-300 font-bold transition-colors">
                        상세 이용 방법 보기 <ChevronRight size={18} />
                    </a>
                </div>
            </div>

            <div id="pricing" class="mb-12 bg-slate-900/30 p-8 rounded-2xl border border-slate-800/50 backdrop-blur-sm hover:border-purple-500/20 transition-colors animate-fade-in-up delay-300">
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                    <div class="w-1.5 h-8 bg-purple-500 rounded-full"></div>
                    4. 가격 안내
                </h2>
                <div class="overflow-x-auto">
                    <table class="w-full text-left border-collapse">
                        <thead>
                            <tr class="border-b border-slate-700 text-purple-400">
                                <th class="py-3 px-4">항목</th>
                                <th class="py-3 px-4">금액</th>
                                <th class="py-3 px-4">비고</th>
                            </tr>
                        </thead>
                        <tbody class="text-slate-300 text-sm">
                            {content.price.baseRates.slice(0, 5).map((rate) => (
                                <tr class="border-b border-slate-800">
                                    <td class="py-3 px-4 font-bold text-white">{rate.label}</td>
                                    <td class="py-3 px-4">{rate.amount}</td>
                                    <td class="py-3 px-4 text-slate-400">{rate.note}</td>
                                </tr>
                            ))}
                        </tbody>
                    </table>
                </div>
                <p class="text-xs text-slate-500 mt-4">* 위 가격은 평균 시세이며, 업소 및 시간대에 따라 변동될 수 있습니다.</p>
                <div class="mt-6 text-center">
                    <a href={`/${region.id}-chaksukppa-guide/price`} class="inline-flex items-center gap-2 text-purple-400 hover:text-purple-300 font-bold transition-colors">
                        전체 가격표 및 상세 안내 <ChevronRight size={18} />
                    </a>
                </div>
            </div>

            <div id="ranking" class="mb-12 animate-fade-in-up delay-300">
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                    <div class="w-1.5 h-8 bg-purple-500 rounded-full"></div>
                    5. {region.name} 착석빠 추천 TOP 3
                </h2>
                <div class="space-y-4">
                    {content.ranking.items.slice(0, 3).map((item) => (
                        <div class="bg-slate-900/50 p-6 rounded-xl border border-slate-800 hover:border-purple-500/50 transition-all">
                            <div class="flex items-start gap-4">
                                <div class="flex-shrink-0 w-12 h-12 bg-purple-500/20 rounded-full flex items-center justify-center">
                                    <span class="text-2xl font-bold text-purple-400">{item.rank}</span>
                                </div>
                                <div class="flex-1">
                                    <h3 class="text-xl font-bold text-white mb-2">{item.name}</h3>
                                    <p class="text-slate-400 text-sm mb-3">{item.description}</p>
                                    <div class="flex flex-wrap gap-2">
                                        {item.highlights.map((highlight) => (
                                            <span class="text-xs bg-purple-500/10 text-purple-400 px-3 py-1 rounded-full border border-purple-500/20">
                                                {highlight}
                                            </span>
                                        ))}
                                    </div>
                                </div>
                            </div>
                        </div>
                    ))}
                </div>
                <div class="mt-6 text-center">
                    <a href={`/${region.id}-chaksukppa-guide/ranking`} class="inline-flex items-center gap-2 text-purple-400 hover:text-purple-300 font-bold transition-colors">
                        전체 랭킹 보기 <ChevronRight size={18} />
                    </a>
                </div>
            </div>

            <div id="tips" class="mb-12 bg-slate-900/50 p-8 rounded-2xl border border-slate-800">
                <h2 class="text-2xl font-bold text-white mb-6 flex items-center gap-3">
                    <Star class="text-purple-500" /> 6. 착석빠 이용 꿀팁
                </h2>
                <ul class="space-y-4 text-slate-300 font-light">
                    {content.price.tips.slice(0, 3).map((tip) => (
                        <li class="flex gap-3">
                            <CheckCircle class="text-purple-500 w-5 h-5 flex-shrink-0 mt-1" />
                            <span>{tip}</span>
                        </li>
                    ))}
                </ul>
            </div>

            <div id="faq" class="grid grid-cols-1 md:grid-cols-2 gap-10 mb-20 animate-fade-in-up delay-300">
                <div class="bg-purple-900/10 p-8 rounded-2xl border border-purple-500/20">
                    <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-2">
                        <HelpCircle class="text-purple-500" /> 7. 자주 묻는 질문 (FAQ)
                    </h3>
                    <div class="space-y-6">
                        {faqList.map((faq) => (
                            <div>
                                <p class="font-bold text-purple-400 mb-1">Q. {faq.question}</p>
                                <p class="text-slate-300 text-sm">A. {faq.answer}</p>
                            </div>
                        ))}
                    </div>
                    <div class="mt-6 text-center">
                        <a href={`/${region.id}-chaksukppa-guide/faq`} class="inline-flex items-center gap-2 text-purple-400 hover:text-purple-300 font-bold transition-colors">
                            더 많은 질문과 답변 보기 <ChevronRight size={18} />
                        </a>
                    </div>
                </div>

                <div class="bg-slate-900/50 p-8 rounded-2xl border border-slate-800">
                    <h3 class="text-2xl font-bold text-white mb-6 flex items-center gap-2">
                        <Sparkles class="text-purple-500" /> 갤러리
                    </h3>
                    <div class="grid grid-cols-2 gap-4">
                        {[4, 5, 6, 7].map((i) => (
                            <div class="aspect-square rounded-lg overflow-hidden border border-slate-800 hover:border-purple-500/50 transition-all duration-300 group">
                                <img src={`/images/partners/partner_${i}.webp`} alt={`${region.name} 착석빠 ${i}`} loading="lazy" width="200" height="200"
                                    class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-500" />
                            </div>
                        ))}
                    </div>
                </div>
            </div>

            <div class="bg-gradient-to-r from-purple-800 to-indigo-900 rounded-3xl p-10 text-center relative overflow-hidden shadow-2xl animate-fade-in-up delay-300">
                <h2 class="text-3xl md:text-4xl font-extrabold text-white mb-6 relative z-10">{region.name} 착석빠 예약 문의</h2>
                <p class="text-purple-100 mb-8 max-w-2xl mx-auto relative z-10">
                    특별한 시간을 보내세요.<br />
                    지금 바로 예약 문의하세요.
                </p>
                <button onclick={`window.location.href = 'tel:${region.phone}'`} class="bg-white text-purple-900 font-bold py-4 px-12 rounded-full shadow-lg hover:scale-105 transition-transform relative z-10 flex items-center gap-2 mx-auto">
                    <Phone fill="currentColor" size={20} /> {region.phone} 간편 예약
                </button>
            </div>

            <RelatedServices region={region} />
        </div>
    </div>
</PageLayout>
