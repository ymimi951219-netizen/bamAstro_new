---
import { PageLayout, LocalBusinessSchema, FAQPageSchema, BreadcrumbSchema, BackToPillar, RelatedPosts } from '@bamastro/ui';
import { region } from '@/config/region';
import { Phone, ChevronRight } from 'lucide-astro';

export function getStaticPaths() {
  return [
    { params: { region: region.id } },
  ];
}

const { region: regionId } = Astro.params;

const faqList = [
    {
        question: `${region.name} 기모노룸 1인 방문도 가능한가요?`,
        answer: "네, 가능합니다. 도심 비즈니스 방문객 이용이 많아 1:1 응대도 부담 없이 안내합니다."
    },
    {
        question: `${region.name} 기모노룸 픽업은 어디까지 지원되나요?`,
        answer: `픽업은 ${region.name} 주요 역 및 중심 상권에서 운영됩니다. 정확한 동선은 예약 시 조율해 드립니다.`
    },
    {
        question: `${region.name} 기모노룸 주대/TC 기준이 궁금해요.`,
        answer: "주대 18만원 이상, TC 10만원 이상 기준이며 시간·코스·인원에 따라 달라질 수 있습니다. 상세는 문의 부탁드립니다."
    },
    {
        question: "기모노룸과 일반 룸살롱의 차이점은 무엇인가요?",
        answer: "기모노룸은 매니저들이 일본 전통 의상(기모노, 유카타)을 착용하고 서비스하는 테마 룸입니다. 이색적인 분위기와 일본식 의전 서비스가 특징입니다."
    },
    {
        question: "비즈니스 접대에 적합한가요?",
        answer: "매우 적합합니다. 이색적인 분위기 덕분에 접대 성공률이 높고, 뻔한 곳이 지겨운 바이어 분들에게 강력 추천합니다."
    },
    {
        question: "사케도 주문할 수 있나요?",
        answer: "네, 준마이, 다이긴죠 등 고급 사케를 구비하고 있습니다. 일본 분위기에 맞게 사케 SET으로 즐기실 수 있습니다."
    },
    {
        question: "예약은 필수인가요?",
        answer: "강력히 권장합니다. 기모노 복장 특성상 환복 및 준비 시간이 필요하므로, 최소 1시간 전 예약을 해주시면 더욱 완벽하게 모실 수 있습니다."
    },
    {
        question: "기모노 쇼는 항상 진행되나요?",
        answer: "업소 상황에 따라 다르지만, 중간 타임에 간단한 인사 및 포토 타임이 진행될 수 있습니다. 예약 시 요청하시면 확인해 드립니다."
    }
];

const seoProps = {
  title: `${region.name} 기모노룸 FAQ | 자주 묻는 질문과 답변`,
  description: `${region.name} 기모노룸 자주 묻는 질문 완벽 해답. 예약·가격·테마·포토존 기본 정보. 전문가가 답변합니다. 이색 체험 필독!`,
  keywords: [`${region.name} 기모노룸 FAQ`, `${region.name} 기모노룸 자주 묻는 질문`, `${region.name} 기모노룸 예약`, `${region.name} 기모노룸 가격`],
};

const relatedItems = [
  {
    title: `${region.name} 기모노룸 완벽 가이드`,
    href: `/${region.id}-kimono-room-guide`,
    description: '기모노룸 시스템, 가격, 추천 업소 정보',
    category: '기모노룸'
  },
  {
    title: `${region.name} 셔츠룸 가이드`,
    href: `/${region.id}-shirtsroom-guide`,
    description: '화이트 셔츠의 매력, 셔츠룸 가이드',
    category: '셔츠룸'
  },
  {
    title: `${region.name} 하이퍼블릭 가이드`,
    href: `/${region.id}-highpublic-guide`,
    description: '프라이빗 펍, 비즈니스 접대에 최적',
    category: '하이퍼블릭'
  },
];
---

<PageLayout {...seoProps} region={region}>
    <LocalBusinessSchema region={region} />
    <BreadcrumbSchema region={region} />
    <FAQPageSchema items={faqList} />

    <div class="pt-24 md:pt-32 min-h-screen bg-transparent px-4 pb-20">
        <div class="container mx-auto max-w-4xl">
            <BackToPillar
                href={`/${region.id}-kimono-room-guide`}
                title={`${region.name} 기모노룸 완벽 가이드`}
                description="가격, 시스템, 추천 업소 정보 모두 보기"
                accentColor="red"
            />

            <div class="text-center mb-16">
                <div class="flex justify-center mb-4">
                    <div class="w-1.5 h-12 bg-red-500 rounded-full"></div>
                </div>
                <h1 class="text-3xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-red-200 to-red-500 mb-6">
                    기모노룸 A to Z (FAQ)
                </h1>
                <p class="text-slate-400 text-lg leading-relaxed max-w-2xl mx-auto">
                    이색 테마의 매력. <strong class="text-white">{region.name} 기모노룸</strong>에 대한 모든 궁금증을 해결해 드립니다.
                </p>
            </div>

            <div class="grid gap-6">
                {faqList.map((item) => (
                    <div class="bg-slate-900/50 p-6 rounded-2xl border border-slate-800 hover:border-red-500/30 transition-all duration-300">
                        <h3 class="text-lg md:text-xl font-bold text-red-400 mb-3 flex items-start gap-2">
                            <span class="bg-red-500/10 p-1 rounded text-sm mt-1">Q</span>
                            {item.question}
                        </h3>
                        <p class="text-slate-300 leading-relaxed pl-8">
                            <span class="font-bold text-slate-500 mr-2">A.</span>
                            {item.answer}
                        </p>
                    </div>
                ))}
            </div>

            <div class="mt-16 text-center">
                <p class="text-slate-400 mb-6">더 궁금한 점이 있으신가요? 24시간 언제든 물어보세요.</p>
                <div class="flex flex-col md:flex-row justify-center gap-4">
                    <button onclick={`window.location.href = 'tel:${region.phone}'`} class="bg-red-600 hover:bg-red-700 text-white font-bold py-4 px-8 rounded-xl flex items-center justify-center gap-2 transition-all shadow-lg hover:shadow-red-500/25">
                        <Phone size={20} /> 실시간 예약 문의
                    </button>
                    <a href={`/${region.id}-kimono-room-guide`} class="bg-slate-800 hover:bg-slate-700 text-white font-bold py-4 px-8 rounded-xl flex items-center justify-center gap-2 transition-all">
                        <ChevronRight size={20} /> 기모노룸 메인으로
                    </a>
                </div>
            </div>

            <RelatedPosts items={relatedItems} title="다른 업종도 확인해보세요" accentColor="rose" />
        </div>
    </div>
</PageLayout>
