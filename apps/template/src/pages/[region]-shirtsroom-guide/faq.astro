---
import { PageLayout, LocalBusinessSchema, FAQPageSchema, BreadcrumbSchema, BackToPillar, RelatedPosts } from '@bamastro/ui';
import { region } from '@/config/region';
import { Phone, ChevronRight } from 'lucide-astro';

export function getStaticPaths() {
  return [
    { params: { region: region.id } },
  ];
}

const { region: regionId } = Astro.params;

const faqList = [
    {
        question: `${region.name} 셔츠룸 1인 방문도 가능한가요?`,
        answer: "네, 가능합니다. 도심 비즈니스 방문객 이용이 많아 1:1 응대도 부담 없이 안내합니다."
    },
    {
        question: `${region.name} 셔츠룸 픽업은 어디까지 지원되나요?`,
        answer: `픽업은 ${region.name} 주요 역 및 중심 상권에서 운영됩니다. 정확한 동선은 예약 시 조율해 드립니다.`
    },
    {
        question: `${region.name} 셔츠룸 주대/TC 기준이 궁금해요.`,
        answer: "주대 18만원 이상, TC 10만원 이상 기준이며 시간·코스·인원에 따라 달라질 수 있습니다. 상세는 문의 부탁드립니다."
    },
    {
        question: "셔츠룸과 일반 룸살롱의 차이점은 무엇인가요?",
        answer: "셔츠룸은 매니저들이 흰색 와이셔츠만 착용하고 서비스하는 컨셉 룸입니다. 초이스 후 셔츠로 환복하는 '인사 타임'이 시그니처입니다."
    },
    {
        question: "비즈니스 접대에 적합한가요?",
        answer: "매우 적합합니다. 어색한 접대 자리를 화기애애하게 만들어 주며, '중요한 손님'이라고 미리 말씀해 주시면 에이스급 매니저를 최우선으로 배정합니다."
    },
    {
        question: "이른 시간 방문하면 혜택이 있나요?",
        answer: "저녁 7시~9시 사이에 방문하시면 주대 할인 이벤트가 적용되는 경우가 많습니다. 가성비를 노리신다면 일찍 오세요."
    },
    {
        question: "예약 없이 방문해도 되나요?",
        answer: `가능하지만 ${region.name} 상권 특성상 피크 타임에는 대기가 발생할 수 있어 예약을 추천드립니다.`
    },
    {
        question: "복장 제한이 있나요?",
        answer: "특별한 드레스 코드는 없습니다. 깔끔한 캐주얼이면 충분하며, 비즈니스 목적이라면 포멀한 복장이 좋습니다."
    }
];

const seoProps = {
  title: `${region.name} 셔츠룸 FAQ | 자주 묻는 질문과 답변`,
  description: `${region.name} 셔츠룸 자주 묻는 질문 완벽 해답. 예약·가격·시스템·1인 방문 기본 정보. 전문가가 답변합니다. 초보자 필독!`,
  keywords: [`${region.name} 셔츠룸 FAQ`, `${region.name} 셔츠룸 자주 묻는 질문`, `${region.name} 셔츠룸 예약`, `${region.name} 셔츠룸 가격`],
};

const relatedItems = [
  {
    title: `${region.name} 셔츠룸 완벽 가이드`,
    href: `/${region.id}-shirtsroom-guide`,
    description: '셔츠룸 시스템, 가격, 추천 업소 정보',
    category: '셔츠룸'
  },
  {
    title: `${region.name} 기모노룸 가이드`,
    href: `/${region.id}-kimono-room-guide`,
    description: '이색 테마, 기모노룸 완벽 가이드',
    category: '기모노룸'
  },
  {
    title: `${region.name} 룸살롱 가이드`,
    href: `/${region.id}-room-salon-guide`,
    description: '고급 비즈니스 접대, 룸살롱 가이드',
    category: '룸살롱'
  },
];
---

<PageLayout {...seoProps} region={region}>
    <LocalBusinessSchema region={region} />
    <BreadcrumbSchema region={region} />
    <FAQPageSchema items={faqList} />

    <div class="pt-24 md:pt-32 min-h-screen bg-transparent px-4 pb-20">
        <div class="container mx-auto max-w-4xl">
            <BackToPillar
                href={`/${region.id}-shirtsroom-guide`}
                title={`${region.name} 셔츠룸 완벽 가이드`}
                description="가격, 시스템, 추천 업소 정보 모두 보기"
                accentColor="blue"
            />

            <div class="text-center mb-16">
                <div class="flex justify-center mb-4">
                    <div class="w-1.5 h-12 bg-blue-500 rounded-full"></div>
                </div>
                <h1 class="text-3xl md:text-5xl font-extrabold text-transparent bg-clip-text bg-gradient-to-r from-blue-200 to-blue-500 mb-6">
                    셔츠룸 A to Z (FAQ)
                </h1>
                <p class="text-slate-400 text-lg leading-relaxed max-w-2xl mx-auto">
                    화이트 셔츠의 매력. <strong class="text-white">{region.name} 셔츠룸</strong>에 대한 모든 궁금증을 해결해 드립니다.
                </p>
            </div>

            <div class="grid gap-6">
                {faqList.map((item) => (
                    <div class="bg-slate-900/50 p-6 rounded-2xl border border-slate-800 hover:border-blue-500/30 transition-all duration-300">
                        <h3 class="text-lg md:text-xl font-bold text-blue-400 mb-3 flex items-start gap-2">
                            <span class="bg-blue-500/10 p-1 rounded text-sm mt-1">Q</span>
                            {item.question}
                        </h3>
                        <p class="text-slate-300 leading-relaxed pl-8">
                            <span class="font-bold text-slate-500 mr-2">A.</span>
                            {item.answer}
                        </p>
                    </div>
                ))}
            </div>

            <div class="mt-16 text-center">
                <p class="text-slate-400 mb-6">더 궁금한 점이 있으신가요? 24시간 언제든 물어보세요.</p>
                <div class="flex flex-col md:flex-row justify-center gap-4">
                    <button onclick={`window.location.href = 'tel:${region.phone}'`} class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-4 px-8 rounded-xl flex items-center justify-center gap-2 transition-all shadow-lg hover:shadow-blue-500/25">
                        <Phone size={20} /> 실시간 예약 문의
                    </button>
                    <a href={`/${region.id}-shirtsroom-guide`} class="bg-slate-800 hover:bg-slate-700 text-white font-bold py-4 px-8 rounded-xl flex items-center justify-center gap-2 transition-all">
                        <ChevronRight size={20} /> 셔츠룸 메인으로
                    </a>
                </div>
            </div>

            <RelatedPosts items={relatedItems} title="다른 업종도 확인해보세요" accentColor="blue" />
        </div>
    </div>
</PageLayout>
