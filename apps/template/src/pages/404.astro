---
import { PageLayout } from '@bamastro/ui';
import { region } from '@/config/region';
import { Home, Phone, ArrowLeft } from 'lucide-astro';

const seoProps = {
  title: `페이지를 찾을 수 없습니다 | ${region.name} 서우실장`,
  description: `요청하신 페이지를 찾을 수 없습니다. ${region.name} 유흥 가이드 메인으로 이동해주세요.`,
  noindex: true,
};
---

<PageLayout {...seoProps} region={region}>
  <div class="min-h-[80vh] flex items-center justify-center px-4">
    <div class="text-center max-w-lg">
      <!-- 404 Number -->
      <div class="relative mb-8">
        <span class="text-[150px] md:text-[200px] font-black text-transparent bg-clip-text bg-gradient-to-br from-purple-500/20 to-fuchsia-500/20 leading-none">
          404
        </span>
        <div class="absolute inset-0 flex items-center justify-center">
          <div class="w-24 h-24 rounded-full bg-purple-500/10 flex items-center justify-center animate-pulse">
            <svg class="w-12 h-12 text-purple-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9.172 16.172a4 4 0 015.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </div>
        </div>
      </div>

      <!-- Message -->
      <h1 class="text-2xl md:text-3xl font-bold text-white mb-4">
        페이지를 찾을 수 없습니다
      </h1>
      <p class="text-slate-400 mb-8 leading-relaxed">
        요청하신 페이지가 삭제되었거나 주소가 변경되었을 수 있습니다.<br />
        아래 버튼을 통해 원하시는 페이지로 이동해주세요.
      </p>

      <!-- Actions -->
      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <a href="/" class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-gradient-to-r from-purple-600 to-fuchsia-600 hover:from-purple-500 hover:to-fuchsia-500 text-white font-medium rounded-xl transition-all shadow-lg shadow-purple-900/30">
          <Home class="w-5 h-5" />
          메인으로 이동
        </a>
        <a href={`tel:${region.phone}`} class="inline-flex items-center justify-center gap-2 px-6 py-3 bg-white/5 hover:bg-white/10 text-white font-medium rounded-xl border border-white/10 hover:border-purple-500/30 transition-all">
          <Phone class="w-5 h-5" />
          전화 문의
        </a>
      </div>

      <!-- Quick Links -->
      <div class="mt-12 pt-8 border-t border-white/5">
        <p class="text-sm text-slate-500 mb-4">인기 페이지</p>
        <div class="flex flex-wrap justify-center gap-2">
          {region.venueTypes.slice(0, 4).map(type => (
            <a href={`/${type.slug}`} class="px-4 py-2 text-sm text-slate-400 hover:text-white bg-slate-800/50 hover:bg-slate-800 rounded-lg transition-colors">
              {type.name}
            </a>
          ))}
        </div>
      </div>
    </div>
  </div>
</PageLayout>
